import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const CustomTabIcon = () => {
  return (
    <View
    style={{
      width: 24, // Customize the width as needed
      height: 24, // Customize the height as needed
      backgroundColor: color, // Use the provided color prop for the background color
      borderRadius: 12, // Make it a circle or adjust the border radius as needed
    }}
  />
  )
}

export default CustomTabIcon

const styles = StyleSheet.create({})